# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MoviesList' do
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!
    
    # Pods for MoviesList
    # Network
    pod 'Alamofire' , '~> 4.5.1'
    pod 'ObjectMapper'
    # Image Cashing
    pod 'SDWebImage', '~> 4.0'
    
    target 'MoviesListTests' do
        inherit! :search_paths
        # Pods for testing
    end
    
    target 'MoviesListUITests' do
        inherit! :search_paths
        # Pods for testing
    end
    
end
